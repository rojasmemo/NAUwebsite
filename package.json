{
  "name": "nau-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --stream -r dev",
    "build": "pnpm -r build",
    "astro": "pnpm --filter nau-astro-site",
    "sanity": "pnpm --filter nau-sanity-studio"
  },
  "pnpm": {
    "overrides": {
      "date-fns": "2.30.0"
    }
  }
}

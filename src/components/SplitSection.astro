---
// src/components/SplitSection.astro

interface Props {
  bgColorClass?: string;
  containerClasses?: string;
  reverseOnMobile?: boolean;
}

const { 
  bgColorClass = 'bg-white',
  containerClasses = 'py-12 md:py-8 px-4 md:px-20',
  reverseOnMobile = false
} = Astro.props;

const leftOrder = reverseOnMobile ? 'order-2 md:order-1' : 'order-1';
const rightOrder = reverseOnMobile ? 'order-1 md:order-2' : 'order-2';
---
<section class:list={["content-section-wrapper", bgColorClass, containerClasses]}>
  <div class="grid md:grid-cols-2 gap-8 items-center px-4 md:px-8 lg:px-16">
    <div class:list={[leftOrder]}><slot name="left" /></div>
    <div class:list={[rightOrder]}><slot name="right" /></div>
  </div>
</section>